<!doctype html>
<html lang="es-MX">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Cómo proteger pisos durante una obra o remodelación (y qué materiales realmente funcionan)</title>
  <meta name="description" content="Guía técnica para contratistas y arquitectos: qué materiales realmente protegen pisos de porcelanato, mármol y cerámica durante una obra activa — y cuánto cuesta equivocarse con la elección incorrecta.">
  <meta name="robots" content="noindex, nofollow">
  <meta property="og:title" content="Cómo proteger pisos durante una obra o remodelación (y qué materiales realmente funcionan)">
  <meta property="og:description" content="Guía técnica para contratistas y arquitectos: qué materiales realmente protegen pisos de porcelanato, mármol y cerámica durante una obra activa — y cuánto cuesta equivocarse con la elección incorrecta.">
  <meta property="og:type" content="website">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@3.19.0/dist/tabler-icons.min.css">
  <style>
    html {
      scroll-behavior: smooth;
      background: #f4f4f4;
    }
    body {
      margin: 0;
      background: #f4f4f4;
      color: #575757;
      font-family: Poppins, Arial, sans-serif;
    }
    main {
      width: min(1040px, calc(100% - 32px));
      margin: 0 auto;
      padding: 40px 0 64px;
    }
    .gh-review-shell {
      background: #ffffff;
      border: 1px solid #e5e5e5;
      border-radius: 18px;
      box-shadow: 0 12px 40px rgba(0,0,0,.08);
      overflow: hidden;
    }
    .gh-review-bar {
      display: flex;
      justify-content: space-between;
      gap: 16px;
      align-items: center;
      padding: 14px 20px;
      background: #383838;
      color: #fff;
      font-size: 13px;
    }
    .gh-review-bar strong {
      font-weight: 700;
    }
    .gh-review-bar span {
      color: rgba(255,255,255,.72);
    }
    .gh-review-content {
      padding: clamp(20px, 4vw, 44px);
    }
    @media (max-width: 640px) {
      main {
        width: min(100% - 20px, 1040px);
        padding: 16px 0 40px;
      }
      .gh-review-bar {
        align-items: flex-start;
        flex-direction: column;
      }
      .gh-review-content {
        padding: 18px;
      }
    }
  </style>
</head>
<body>
  <main>
    <section class="gh-review-shell">
      <div class="gh-review-bar">
        <strong>CubrePack · Pieza 2</strong>
        <span>Vista de revisión para cliente</span>
      </div>
      <div class="gh-review-content">
<!--
SEO title: Cómo proteger pisos durante una obra o remodelación (y qué materiales realmente funcionan)
Meta description: Guía técnica para contratistas y arquitectos: qué materiales realmente protegen pisos de porcelanato, mármol y cerámica durante una obra activa — y cuánto cuesta equivocarse con la elección incorrecta.
URL objetivo: /como-proteger-pisos-en-obra
-->
<style id="cp-pieza2-css">
:root {
  --cp-red: #dc2e2e;
  --cp-red-dark: #a81c1c;
  --cp-red-light: #fff0f0;
  --cp-dark: #383838;
  --cp-text: #575757;
  --cp-muted: #8a8a8a;
  --cp-soft: #f5f5f5;
  --cp-border: #e5e5e5;
  --cp-white: #ffffff;
  --cp-green: #2e7d32;
  --cp-green-light: #e8f5e9;
  --cp-orange: #e65100;
  --cp-radius: 8px;
  --cp-radius-lg: 12px;
  --cp-shadow: 0 4px 20px rgba(0,0,0,.08);
  --cp-shadow-hover: 0 8px 32px rgba(0,0,0,.14);
  --cp-transition: all .22s cubic-bezier(.4,0,.2,1);
}

.cp-pieza2 {
  font-family: Poppins, Arial, sans-serif;
  color: var(--cp-text);
  font-size: 16px;
  line-height: 1.75;
}
.cp-pieza2 * { box-sizing: border-box; }

.cp-pieza2 h1,
.cp-pieza2 h2,
.cp-pieza2 h3,
.cp-pieza2 h4 {
  font-family: Poppins, Arial, sans-serif;
  color: var(--cp-dark);
  font-weight: 700;
  letter-spacing: -0.015em;
}
.cp-pieza2 h1 {
  font-size: clamp(2rem, 4vw, 2.85rem);
  line-height: 1.12;
  margin: 0 0 1.5rem;
  padding-bottom: 1.25rem;
  border-bottom: 3px solid var(--cp-red);
}
.cp-pieza2 h2 {
  font-size: clamp(1.45rem, 2.6vw, 2rem);
  line-height: 1.25;
  margin: 3.5rem 0 1.25rem;
  padding-left: 1rem;
  border-left: 5px solid var(--cp-red);
}
.cp-pieza2 h3 {
  font-size: 1.13rem;
  line-height: 1.35;
  margin: 1.75rem 0 .6rem;
}
.cp-pieza2 p { margin: 0 0 1.15rem; }
.cp-pieza2 strong { color: var(--cp-dark); font-weight: 700; }
.cp-pieza2 a { color: var(--cp-red); text-decoration: none; font-weight: 600; }
.cp-pieza2 a:hover { color: var(--cp-red-dark); text-decoration: underline; }

.cp-block { margin: 2.5rem 0; }

/* ── EYEBROW LABELS ── */
.cp-eyebrow {
  color: var(--cp-red);
  font-size: .72rem;
  font-weight: 700;
  letter-spacing: .1em;
  text-transform: uppercase;
  margin-bottom: .75rem;
  display: block;
}

/* ══════════════════════════════════════
   CALLOUT
══════════════════════════════════════ */
.cp-callout {
  border-left: 6px solid var(--cp-red);
  background: var(--cp-soft);
  padding: 1.75rem 2rem;
  border-radius: 0 var(--cp-radius) var(--cp-radius) 0;
  box-shadow: var(--cp-shadow);
}
.cp-callout p:last-child { margin-bottom: 0; }

/* ══════════════════════════════════════
   STATS ROW
══════════════════════════════════════ */
.cp-stats-row {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1px;
  background: var(--cp-border);
  border: 1px solid var(--cp-border);
  border-radius: var(--cp-radius-lg);
  box-shadow: var(--cp-shadow);
  overflow: hidden;
}
.cp-stat-item {
  background: var(--cp-white);
  padding: 2rem 1.5rem;
  text-align: center;
  transition: background .2s;
}
.cp-stat-item:hover { background: var(--cp-red-light); }
.cp-stat-num {
  font-size: clamp(2rem, 4.5vw, 3rem);
  font-weight: 700;
  color: var(--cp-red);
  line-height: 1;
  display: block;
  margin-bottom: .5rem;
}
.cp-stat-lab